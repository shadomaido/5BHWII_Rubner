Dieses Beispiel zeigt eine verkettete Liste, welche unter anderem einen iterator vorfindet.
yield wurde verwendet, um bei dem iterator die while-Schleife nicht zu unterbrechen.

in der Datei "main" sehen sie die Anwendung der verketteten Liste mit ein paar Beispielen.

imports:
random, um zufällige Einträge zu erzeugen.